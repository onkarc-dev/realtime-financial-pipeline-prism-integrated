# Windows Fix and Run Guide\n\nThis patch fixes two Windows build blockers:\n\n1. `benchmark_engine.cpp` now includes `<string>` so `std::stoull` compiles with MSVC.\n2. `CMakeLists.txt` now detects vcpkg's libwebsockets target: `unofficial::libwebsockets::libwebsockets`.\n\n## Clean Build\n\nRun from the project root:\n\n```cmd\ncd C:\Users\Admin\realtime-financial-pipeline\nrmdir /s /q build\nmkdir build\ncd build\ncmake .. -DCMAKE_BUILD_TYPE=Release -DCMAKE_TOOLCHAIN_FILE=C:\vcpkg\scripts\buildsystems\vcpkg.cmake\ncmake --build . --config Release\n```\n\n## Run Benchmark\n\n```cmd\nRelease\benchmark_engine.exe 1000000 1000000\n```\n\n## Run Live Binance\n\n```cmd\nRelease\live_binance_engine.exe btcusdt\n```\n\nExpected output every second:\n\n```text\nlive_rate_msg_s=... total_processed=... ws_received=... last_price=...\n```\n\n## Kafka Note\n\nYour Kafka binaries compiled correctly. The runtime error means no Kafka broker is running at `localhost:9092`. Start Docker Desktop, then from project root run:\n\n```cmd\ndocker compose up -d\n```\n\nThen run:\n\n```cmd\nRelease\kafka_consumer.exe localhost:9092 raw-trades\nRelease\kafka_producer.exe localhost:9092 raw-trades 100000\n```\n